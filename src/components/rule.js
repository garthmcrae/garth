const code = `
<Rule />
`;

const object = {
  code,
  description:
    "Indicates a thematic break between content elements. Used between content on this page.",
  name: "Rule",
};

export default object;
